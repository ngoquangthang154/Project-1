$("tr").find("td:first").each(function(){
  if ($(this).text() == ("<%= raw @id_p %>")) {
    $(this).parent().remove();
  }
});
$("#total_sum").text("$ <%= raw @sum_cart %>");
$("#txt_total").val("$ <%= raw @sum_cart %>");
$("#cout_pc").text("<%= raw session[:cart_p].count %>");
