<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, @trs] do |f| %>

      <%= f.label :name, t("products.new.name") %>
      <%= f.text_field :name, class: "form-control" %>

      <%= f.submit t("save.save"), class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
